# OCGS Framework — Claude Code Harness Example

OCGS content is in the `.agents/` directory:
- Agents: `.agents/agents/`
- Skills: `.agents/skills/`
- Commands: `.agents/commands/`
- Rules: `.agents/rules/`

This is a sample config for future Claude Code harness integration.
See `.pi/extensions/` for the Pi implementation reference.
